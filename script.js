// Simple script to handle form submission (mock)
document.querySelector('form').addEventListener('submit', function(e) {
    e.preventDefault(); // Prevent the default form submission
    alert('Merci pour votre message !');
});
